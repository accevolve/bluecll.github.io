{
  // ESA Pages 部署配置：构建时仅生成 IndexNow 验证文件，仓库根目录即站点根。
  // assets.directory 会覆盖控制台「静态资源目录」，避免再次配错（须为目录而非文件）。
  "name": "jsontool",
  "buildCommand": "node scripts/prepare-indexnow-key.mjs",
  "assets": {
    "directory": "."
  }
}
